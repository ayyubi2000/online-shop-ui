import { useState, memo } from "react";
import { ReactPropTypes } from "react";
const Inline =({value}) =>{
    console.log('inline render buldi');
    let box =0;
    for (let i = 0; i < 100000; i++){
        box = i;
        
    }
 }